# Screenshots

Drop PNGs here with these names and they'll render in the root `README.md`:

- `dashboard.png` — the dashboard with metrics + recent workflows
- `workflow-detail.png` — a workflow detail page (timeline + gates + approval)
- `artifact.png` — a generated PR artifact page

Suggested capture: run `pnpm dev`, open each page at ~1440px wide, and screenshot
the content area.
